$kind: http-request
url: "{{base_url}}/api/user-order/track-order"
method: POST
headers:
  Authorization: Bearer {{token}}
body:
  type: formdata
  content:
    - type: text
      key: order_id
      value: "1"
order: 4000
